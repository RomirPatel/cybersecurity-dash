import React from 'react';

function GoogleAuth() {
    return (
        <div className="google-auth">
            {/* Google Auth button and logic here */}
            <button>Sign in with Google</button>
        </div>
    );
}

export default GoogleAuth;
